<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        h1 {
            text-align: center;
            color: black;
        }
        body {
            border: 2px solid black;
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 20px auto;
            background-color: #1E2761;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            border-bottom: 2px solid black;
            margin-bottom: 20px;
            padding: 10px;
        }
        a {
            background-color: blue;
            color: white;
            display: inline-block;
            margin-top: 10px;
            padding: 10px 15px;
        }
        a:hover {
            background-color: rgb(108, 151, 251);
        }
        .content {
            background-color: #CADCFC;
            padding: 40px;
            box-sizing: border-box;
        }
        
    </style>
</head>
<body>
    <div class="content">
        <h1><%= title %></h1>

        <% if (isDetailPage) { %>
            <p>User: <%= messages[0].username %></p>
            <p>Message: <%= messages[0].message_text %></p>
            <p>Added on: <%= new Date(messages[0].added).toLocaleString() %></p>
            <br>
            <a href="/">Back</a>
        <% } else { %>
            <ul>
                <% messages.forEach((message) => { %>
                    <li>
                        <%= message.username %>: <%= message.message_text %> 
                        <em>(<%= new Date(message.added).toLocaleString() %>)</em>
                        <a href="/messages/<%= message.Message_ID %>">Open</a>
                    </li>
                <% }); %>
            </ul>

            <a href="/new">Add a new message</a>
        <% } %>
    </div>
</body>
</html>
